# Docker Environment Variables
NODE_ENV=development
PORT=5000

# Database Configuration (using local MongoDB container)
MONGODB_URI=mongodb://mongodb:27017/selvedge-mark

# JWT Configuration
JWT_SECRET=selvedge_mark_secret_key
JWT_EXPIRE=30d